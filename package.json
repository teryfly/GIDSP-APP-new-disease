{
  "name": "dhis2-disease-monitoring",
  "version": "1.0.0",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build && npm run copy-manifest",
    "copy-manifest": "cp manifest.webapp build/",
    "package": "cd build && zip -r ../dhis2-app.zip ./*",
    "build-and-package": "npm run build && npm run package",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^6.20.0",
    "antd": "^5.12.0",
    "moment": "^2.29.4"
  },
  "devDependencies": {
    "@types/react": "^18.2.0",
    "@types/react-dom": "^18.2.0",
    "@vitejs/plugin-react": "^4.2.0",
    "typescript": "^5.3.0",
    "vite": "^5.0.0"
  }
}