import { Space, Typography} from 'antd';

const { Title } = Typography;

const Dashboard = () => {
    return (
        <Space direction="vertical" size="large" style={{ width: '100%' }}>
          <Title>临时CaseList界面</Title>
        </Space>
    );
};

export default Dashboard;